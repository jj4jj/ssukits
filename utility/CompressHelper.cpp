#include "CompressHelper.h"

int CompressHelper::Compress(int iMethod , const Buffer & in, Buffer & out,int param)
{
    //todo
    return -1;
}
int CompressHelper::UnCompress(int iMethod , const Buffer & in, Buffer & out,int param)
{
    //todo
    return -1;
}
